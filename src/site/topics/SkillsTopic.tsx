import { SkillsCarousel } from '../parts/SkillsCarousel'

/** O mesmo carrossel de antes, agora dentro de uma mensagem. */
export function SkillsTopic() {
  return (
    <div className="topic topic-skills">
      <SkillsCarousel />
    </div>
  )
}
